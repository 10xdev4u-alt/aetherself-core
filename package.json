{
  "name": "aetherself-core",
  "version": "0.1.0",
  "private": true,
  "description": "Your AI Identity Layer — portable, encrypted, user-owned",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.17.0"
}
